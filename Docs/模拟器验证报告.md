# iPhone 13 Pro Max 模拟器验证报告

[English](Simulator-Validation-Report.md) | **简体中文**

验证日期：2026-08-15

## 环境

- Mac：Apple Silicon macOS 开发机
- Xcode：26.3（17C529）
- Simulator Runtime：iOS 26.3.1（23D8133，arm64）
- 模拟设备：iPhone 13 Pro Max（iPhone14,3）
- App 配置：Debug，最低部署版本 iOS 16.0

## 测试数据

使用 `TestImages/` 中 6 张图片：

- 宽度范围：640–2400 px
- 高度范围：1500–18000 px
- 包含窄图、宽图、短图、长图和超长图
- 文件创建时间与文件名顺序刻意打乱

视频测试通过 `ffmpeg` 生成 4 个 8 秒 H.264 夹具：

- 容器格式：MP4、MOV、M4V、MPEG-TS
- 层级：根目录 1 个视频、直属子目录 1 个、二级子目录 1 个、同级目录 1 个
- 额外放入文本文件，验证非视频文件被排除

## 自动化步骤

执行：

```bash
./Tools/RunSimulatorSmokeTest.sh
```

脚本完成以下操作：

1. 创建并启动 iPhone 13 Pro Max 模拟器。
2. 使用 `xcodebuild` 完成 Debug 模拟器构建。
3. 安装 `LongImageViewer.app`。
4. 在根目录和两层子目录中生成 MP4、MOV、M4V、TS 文件。
5. 通过正式安全作用域书签和扫描路径关联视频根目录。
6. 验证 4 个视频行、4 个目录行、3 个目录路径及最大 3 层侧边栏深度。
7. 验证非视频文件被忽略，且 App 的 Application Support 目录中没有视频副本。
8. 隐藏一个视频文件和一个子目录，再解除根目录关联，并确认全部源视频仍存在。
9. 启动视频播放，模拟 App 失去活动状态，验证播放器 rate 从 1 变为 0。
10. 在主根目录放 2 张图、两个直属子目录各放 2 张图，并加入空子目录、非图片文件和三级目录图片。
11. 通过正式批量添加接口，一次将两个测试图片目录保存为文件夹来源。
12. 再次批量添加相同目录，验证按路径去重且统一同步图片元数据。
13. 验证根目录优先、直属子目录排序，以及非图片和三级目录文件被排除。
14. 在 18 秒内跨目录边界从第一页连续滚动到第六页。
15. 随滚动位置按需生成图片图块。
16. 记录帧统计、图块数、访问页数、内存、目录顺序和四种排序结果。
17. 删除并恢复一张源图，验证目录增量同步。
18. 验证图片二级侧边栏、子目录定位和当前目录高亮。
19. 将主根目录从 5 个可见条目折叠为 2 个，再展开恢复为 5 个，并确认当前页面不变。
20. 在两个已添加根目录间切换，验证浏览范围与页数同步变化。
21. 删除第二根目录，验证 App 关联和缓存被清理、源文件仍存在。
22. 展示连续选择图片根目录进度页，验证“继续选择”和统一导入入口可见。
23. 验证密码设置、错误密码拒绝、密码修改、旧密码失效和密码关闭。
24. 验证后台 179 秒无需密码、达到 180 秒要求密码的边界。
25. 安装测试密码后重新启动，验证默认锁定页覆盖主浏览页面。
26. 分别以英文、简体中文、日文启动，验证两个媒体标签及进程内语言切换。

## 验证结果

### 功能

- 构建、安装和启动：通过
- 侧边栏可在“图片浏览”和“视频播放”之间切换：通过
- 视频根目录通过持久化安全作用域书签直接访问：通过
- 成功索引 MP4、MOV、M4V、TS 四种视频容器
- 视频目录递归包含两层子目录：通过
- 视频侧边栏包含 4 个目录行和 4 个视频行：通过
- 目录行仅展开或折叠，视频行才触发播放：通过
- 隐藏一个视频文件和一个子目录后全部源数据仍存在：通过
- 解除视频根目录关联后全部源数据仍存在：通过
- 重新关联与恢复隐藏项目路径：通过
- Application Support 中没有源视频副本：通过
- App 失去活动状态后 AVPlayer rate 从 1 变为 0：通过
- 6 张图片导入：通过
- 文件夹书签保存与恢复：通过
- 一次批量添加两个文件夹：通过
- 重复批量添加不产生重复文件夹：通过
- 连续选择后的“继续选择”及统一导入入口：通过
- 仅扫描根目录和直属子目录：通过
- 非图片文件自动忽略：通过
- 三级目录图片不进入浏览范围：通过
- 根目录图片排在直属子目录图片之前：通过
- 直属子目录按本地化文件名顺序排列：通过
- 相邻目录间连续滚动：通过
- 侧边栏展示根目录、缩进的直属子目录及图片数量：通过
- 当前目录高亮随定位和滚动更新：通过
- 点击子目录定位到第 3 页且完整 6 页序列不变：通过
- 子目录第一张图的前一页属于根目录：通过
- 根目录展开箭头可折叠 3 个二级目录行：通过
- 重新展开后恢复全部 5 个侧边栏条目：通过
- 浏览二级目录时折叠仍停留第 3 页，并由一级目录承接高亮：通过
- 主文件夹浏览范围为 6 张：通过
- 第二文件夹浏览范围为 2 张：通过
- 删除第二文件夹后自动回退主文件夹：通过
- 删除文件夹不影响源目录：通过
- 源目录删除一张图后增量同步为 5 张：通过
- 恢复源图片后同步回 6 张：通过
- 图片适应屏幕宽度：通过
- 上下连续滚动：通过
- 超长图片内部切片无黑缝：通过
- 不同文件之间显示 1 个物理像素分割线：通过
- 顶部页码从 `1/6` 更新到 `6/6`：通过
- 顶部文件名随当前页更新：通过
- 右上角进程内存每秒更新：通过
- 创建时间顺排/倒排：通过
- 文件名顺排/倒排：通过
- 未设置密码时直接进入：通过
- 密码摘要写入 Keychain：通过
- 正确密码验证与错误密码拒绝：通过
- 修改密码后旧密码失效、新密码生效：通过
- 验证当前密码后关闭密码：通过
- 设置密码后重新启动显示锁定页：通过
- 离开不足 3 分钟无需密码：通过
- 离开达到 3 分钟要求密码：通过
- 英文界面资源与冷启动：通过
- 简体中文界面资源与冷启动：通过
- 日文界面资源与冷启动：通过
- 同一进程内切换后“来源”、排序菜单和视频标签立即更新：通过
- 所选语言持久化：通过
- iOS arm64 Release 构建：通过
- Mac Catalyst arm64 Release 构建：通过

### 稳定态滚动性能

- 测试时长：18.02 秒
- 记录帧数：1081
- 平均帧率：60.0 FPS
- 超过 22ms 的帧：0
- 访问页数：6/6
- 滚动结束进程内存：约 79.2 MiB

稳定态结果文件：

`Artifacts/Simulator/warm-smoke-test-result.json`

### 冷启动滚动性能

包含首次图片解码的冷启动滚动测试结果：

- 双根目录、二级结构元数据扫描：0.20 秒
- 预期图块总数：30
- 滚动前已生成图块：2
- 滚动到末页后生成图块：30
- 平均帧率：60.0 FPS
- 超过 22ms 的帧：0
- 访问页数：6/6
- 峰值进程内存：约 112.0 MiB
- 结束进程内存：约 85.8 MiB

打开目录后仅生成当前视口附近的 2/30 个图块。主浏览顺序为根目录、`01_Chapter_A`、`02_Chapter_B`，每个目录 2 张图；`notes.txt`、`metadata.json` 和 `DeepIgnored/` 下的图片均未进入 manifest。点击 `01_Chapter_A` 会定位到第 3 页，同时保留完整 6 页序列。稳定态回归为 60.0 FPS、0 慢帧。

## 产物

- `Artifacts/Simulator/first-page.png`：第一页
- `Artifacts/Simulator/scrolling.png`：滚动中
- `Artifacts/Simulator/completed.png`：第六页末尾
- `Artifacts/Simulator/warm-scrolling.png`：稳定态滚动中
- `Artifacts/Simulator/running-now.png`：当前模拟器第一页
- `Artifacts/Simulator/folder-sidebar.png`：主文件夹高亮
- `Artifacts/Simulator/folder-sidebar-child.png`：第 3 页直属子目录高亮
- `Artifacts/Simulator/folder-sidebar-collapsed.png`：停留第 3 页时折叠主根目录
- `Artifacts/Simulator/folder-sidebar-secondary.png`：第二文件夹高亮
- `Artifacts/Simulator/smoke-test-result.json`：冷启动结果
- `Artifacts/Simulator/warm-smoke-test-result.json`：稳定态结果
- `Artifacts/Simulator/folder-removal-result.json`：目录删除增量同步结果
- `Artifacts/Simulator/folder-selection-result.json`：文件夹切换结果
- `Artifacts/Simulator/folder-management-result.json`：文件夹清理结果
- `Artifacts/Simulator/child-directory-result.json`：子目录定位及上一目录结果
- `Artifacts/Simulator/folder-collapse-result.json`：折叠、展开、条目数和页面保持结果
- `Artifacts/Simulator/folder-batch-progress.png`：连续选择两个文件夹后的统一导入入口
- `Artifacts/Simulator/app-lock.png`：应用密码锁定页
- `Artifacts/Simulator/video-sidebar.png`：包含多层目录和视频文件的视频标签
- `Artifacts/Simulator/video-player-paused.png`：完成后台暂停断言后的播放器
- `Artifacts/Simulator/video-library-result.json`：视频索引、层级、源文件保留和暂停结果
- `Artifacts/Simulator/app-lock-result.json`：启动锁定状态结果
- `Artifacts/Simulator/password-store-result.json`：密码设置、校验、修改和关闭结果
- `Artifacts/Simulator/localization-en.json`：英文文案验证
- `Artifacts/Simulator/localization-zh-Hans.json`：简体中文文案验证
- `Artifacts/Simulator/localization-ja.json`：日文文案验证
- `Artifacts/Simulator/localization-persisted.json`：重启后的语言持久化验证
- `Artifacts/Simulator/language-switch-result.json`：进程内语言切换验证
- `Artifacts/Simulator/language-en.png`：英文批量选择页面
- `Artifacts/Simulator/language-zh-Hans.png`：简体中文批量选择页面
- `Artifacts/Simulator/language-ja.png`：日文批量选择页面
